# ============================================
# 🚀 VENDAPLANO - CONFIGURAÇÕES DO SERVIDOR
# ============================================

# Porta do servidor
PORT=3000

# Ambiente (development | production)
NODE_ENV=development

# ============================================
# 💾 BANCO DE DADOS
# ============================================

# Caminho do banco SQLite
DB_PATH=./database/vendas.db

# ============================================
# 🔐 SEGURANÇA E AUTENTICAÇÃO
# ============================================

# Chave secreta para JWT (MUDE ISSO EM PRODUÇÃO!)
# Gere uma chave forte: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=vendaplano_secret_key_2024_MUDE_ISSO_EM_PRODUCAO

# Tempo de expiração do token (exemplos: 1h, 12h, 24h, 7d)
JWT_EXPIRE=24h

# ============================================
# 👤 ADMIN PADRÃO
# ============================================
# Estas credenciais são usadas apenas na inicialização do banco
# IMPORTANTE: Altere a senha após o primeiro login!

ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
ADMIN_EMAIL=admin@vendaplano.com.br

# ============================================
# 🌐 CORS - ORIGENS PERMITIDAS
# ============================================
# Separe múltiplas origens com vírgula
# Em produção, especifique apenas os domínios necessários

CORS_ORIGIN=http://localhost:3000,http://localhost:5500,http://127.0.0.1:5500

# ============================================
# ⚙️ CONFIGURAÇÕES ADICIONAIS
# ============================================

# Rate limiting (requisições por janela de tempo)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Log detalhado de erros (apenas em development)
VERBOSE_ERRORS=true

